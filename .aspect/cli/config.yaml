configure:
  languages:
  plugins:
lint:
  aspects:
    - //tools/lint:linters.bzl%pmd

plugins:
    - name: fix-visibility
      from: github.com/aspect-build/plugin-fix-visibility
      version: v0.1.0
